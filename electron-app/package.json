{
  "name": "electron-app",
  "version": "0.1.0",
  "main": "dist/main.js",
  "private": true,
  "scripts": {
    "start": "tsc && electron .",
    "build": "webpack -p",
    "package": "electron-builder build --dir",
    "package-pl": "electron-builder build --config electron-builder.pl.yml --dir",
    "dist": "electron-builder build --publish never",
    "dist-pl": "electron-builder build --config electron-builder.pl.yml --publish never"
  },
  "devDependencies": {
    "@types/node": "^12.6.9",
    "electron": "^6.0.0",
    "electron-builder": "^21.2.0",
    "ts-loader": "^6.0.4",
    "typescript": "^3.5.3",
    "webpack": "^4.39.1",
    "webpack-cli": "^3.3.6"
  },
  "workspaces": {
    "nohoist": [
      "electron",
      "electron/**"
    ]
  },
  "dependencies": {}
}
